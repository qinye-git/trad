# 选股结果右侧详情面板方案

## 1. 方案目标

在“筛选结果”页中，点击任意股票后，右侧展开详情面板，帮助用户在最短时间内完成一次复核判断。

这次方案按“轻量决策”原则收敛，只回答三个问题：

1. 这只股票现在还值不值得继续看
2. 它为什么会被选中
3. 我现在更适合关注、等待，还是先跳过

## 2. 展示原则

- 只显示会影响决策的数据
- 先给结论，再给证据
- 首屏不堆盘口细节
- V1 先做 `日K / 周K / 月K`，`分时` 放到下一阶段

## 3. V1 首屏最终字段

V1 首屏只显示以下内容：

- 股票名称 / 代码
- 最新价 / 涨跌幅
- 评级 / 得分
- 是否站上 MA20
- 5日超额收益
- 10日上涨天数
- ADV5 成交额
- PE / PB 与行业估值对比
- 一句结论：`继续关注 / 等回踩 / 暂不关注`
- `日K / 周K / 月K`

以下信息不进入 V1 首屏：

- 今开 / 昨收 / 最高 / 最低
- 委比 / 委差
- 过多盘口字段
- 过多解释性文案

## 4. 任务拆分

### 任务 1：右侧详情面板外壳

**功能描述**

在结果页右侧增加详情面板容器。未选中股票时不显示，点击结果行后展开。面板需要有关闭按钮、空态、加载态。

**如何执行**

1. 修改 `app/index.html`，在结果页主区域中加入详情面板 DOM
2. 为面板增加收起、展开、加载、空态的样式
3. 面板宽度先固定在 `440 ~ 520px`
4. 默认不显示，等待选中股票后再展开

**验收标准**

1. 打开应用后，结果页布局正常
2. 未选中股票时，不占用右侧可视空间
3. 选中股票后，右侧面板可以展开
4. 面板关闭后，结果表恢复正常宽度
5. 不影响现有日志页和摘要页

### 任务 2：结果表点击联动

**功能描述**

用户点击筛选结果中的某一行后，打开该股票的右侧详情面板，并高亮当前行。

**如何执行**

1. 在 `app/renderer.js` 的结果表渲染逻辑里，为每一行补充 `data-code`
2. 给 `results-tbody` 做事件代理点击，不给每行单独绑定事件
3. 点击后记录当前 `selectedCode`
4. 为选中行增加高亮样式
5. 点击其他股票时，仅刷新面板内容，不重新收起

**验收标准**

1. 点击任意结果行都能打开正确股票详情
2. 当前选中行有明确高亮
3. 切换到另一只股票时，面板保持展开
4. 不会出现点击 A 却显示 B 的情况

### 任务 3：顶部摘要区

**功能描述**

展示最核心的识别信息和即时判断信息，让用户先知道“我看的是什么”“它现在强不强”。

首屏保留：

- 名称 / 代码
- 最新价 / 涨跌幅
- 评级 / 得分
- 行业（弱化展示）

**如何执行**

1. 在 `app/main.js` 新增 `fetch-stock-detail`
2. 基础字段从 `data/output/qscreen_all_a.json` 或 `qscreen_all_a_meta.json` 中查找
3. 最新价 / 涨跌幅复用现有 `fetch-stock-quotes`
4. 在 `app/preload.js` 暴露新接口
5. 在 `app/renderer.js` 中渲染顶部摘要区

**验收标准**

1. 名称、代码、价格、涨跌幅显示正确
2. 评级、得分与筛选结果一致
3. 行业信息显示但不抢占主要视觉层级
4. 数据加载失败时有明确提示，不报错白屏

### 任务 4：决策摘要区

**功能描述**

这一块是整个详情面板最重要的部分。它不是展示原始数据，而是帮助用户快速判断“现在还值不值得继续看”。

首批保留字段：

- 是否站上 MA20
- 5日是否继续跑赢基准
- 量能是否足够
- 当前价格位置是否偏高
- 一句结论：`继续关注 / 等回踩 / 暂不关注`

**如何执行**

1. 复用 `picked[*].metrics`
2. 用 `close` 和 `MA20` 判断趋势是否还在
3. 用 `ret_5d - idx_ret_5d` 判断是否仍跑赢基准
4. 用 `ADV5_amount` 判断量能是否达到可参与标准
5. 在主进程拼装一句轻量 `decision`
6. 前端只负责显示，不重复堆判断逻辑

**验收标准**

1. 决策摘要区只显示最重要的 4~5 个判断项
2. 用户能在 10 秒内完成“继续看 / 先跳过”的判断
3. 一句结论和底层指标方向一致，不自相矛盾
4. 没有出现大段说明文字堆积

### 任务 5：入选原因区

**功能描述**

告诉用户这只股票为什么会被选中，但只保留真正影响判断的核心证据，不做大而全解释。

保留字段：

- 5日超额收益
- MA20 状态
- 10日上涨天数
- ADV5 成交额
- PE / PB
- 行业估值对比

**如何执行**

1. 从 `picked[*].metrics` 中读取现有字段
2. 用简短标签或信息卡展示，不用长段落解释
3. 若 `PE_industry_ttm` 可用，则展示“低于行业 / 接近行业 / 高于行业”
4. 若个别字段缺失，直接降级显示 `--`

**验收标准**

1. 用户能一眼看出入选的主要原因
2. 展示字段不超过 6 项
3. 缺失字段不会导致界面错乱
4. 信息表达清楚，但不过度解释

### 任务 6：日K / 周K / 月K 图表

**功能描述**

图表的目标不是看盘，而是帮助用户快速看清趋势位置和所处阶段。

V1 只保留：

- 日K
- 周K
- 月K

默认打开 `日K`。

**如何执行**

1. 在 `app/main.js` 新增 `fetch-stock-kline`
2. `day` 直接复用现有日线抓取能力
3. `week` 和 `month` 由日线聚合生成
4. 在前端增加图表标签切换
5. 图表先用轻量实现，不引入重型依赖

**验收标准**

1. 日 / 周 / 月标签切换正常
2. 图表数据与当前股票对应，不串股
3. 图表加载失败时能显示错误态或空态
4. 图表区域不喧宾夺主，仍以决策摘要为主

### 任务 7：请求状态与缓存

**功能描述**

保证切换股票时不会串数据，常用数据不会重复请求，界面能保持流畅。

**如何执行**

1. 在 `renderer.js` 中增加：
   - `selectedCode`
   - `visible`
   - `activeTab`
   - `loading`
   - `requestId`
   - `detailCache`
2. 用 `requestId` 防止旧请求覆盖新请求
3. 在主进程增加轻量缓存：
   - `detail` 缓存 15 秒
   - `day/week/month kline` 缓存 5 分钟
4. 切换股票时先显示加载态，再落最终结果

**验收标准**

1. 快速切换多只股票时不串数据
2. 相同股票重复点击时响应明显更快
3. 加载过程中有明确反馈
4. 不出现旧请求覆盖新结果的问题

### 任务 8：分时作为 V1.1 扩展

**功能描述**

分时属于盘中观察信息，不是 V1 的主链路。放在第二阶段补充，不阻塞首版上线。

**如何执行**

1. 在 `fetch-stock-kline` 中新增 `intraday`
2. 接入分钟级数据源
3. 前端增加 `分时` 标签
4. 分时获取失败时优雅降级，不影响其他标签

**验收标准**

1. 分时有数据时可正常显示
2. 分时无数据时不影响日 / 周 / 月图表
3. V1 不依赖分时即可正常交付

## 5. 文件改动清单

### 必改文件

- `app/index.html`
- `app/renderer.js`
- `app/preload.js`
- `app/main.js`

### 可选拆分文件

- `app/detail_panel.js`
- `app/detail_chart.js`

首版如果追求落地速度，可以先不拆文件，功能稳定后再整理。

## 6. 整体验收标准

满足以下条件，即可认为 V1 方案落地完成：

1. 点击结果行后，右侧详情面板可正常展开
2. 顶部摘要区显示正确
3. 决策摘要区能给出轻量结论
4. 入选原因区只展示最重要的数据
5. 日 / 周 / 月图表可以正常切换
6. 切换股票时不串数据
7. 整个面板保持轻量，没有演变成信息堆叠页

## 7. 推荐执行顺序

建议按下面顺序做：

1. 任务 1：右侧详情面板外壳
2. 任务 2：结果表点击联动
3. 任务 3：顶部摘要区
4. 任务 4：决策摘要区
5. 任务 5：入选原因区
6. 任务 6：日K / 周K / 月K 图表
7. 任务 7：请求状态与缓存
8. 任务 8：分时作为 V1.1 扩展

## 8. 结论

这个方案已经按你要的方式收成执行文档了，后面可以直接逐任务推进。

核心原则不变：

- 少即是多
- 每个字段都要服务于投资判断
- 首版先把最短决策链路做通
